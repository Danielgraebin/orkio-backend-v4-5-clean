# ORKIO v4.5 Backend - Arquivo de Configuração de Exemplo
# Copie este arquivo para .env e preencha com seus valores reais

# ==================== DATABASE ====================
# Supabase Transaction Pooler (porta 6543)
# Formato: postgresql+psycopg://USER:PASSWORD@HOST:6543/postgres
DATABASE_URL=postgresql+psycopg://postgres:password@localhost:6543/postgres

# ==================== AUTHENTICATION ====================
# Chave secreta para assinar tokens JWT
# Gere com: openssl rand -hex 32
JWT_SECRET=seu_jwt_secret_super_seguro_aqui_minimo_32_caracteres

# ==================== LLM / OPENAI ====================
# Chave da API OpenAI
OPENAI_API_KEY=sk-seu_token_aqui

# Modelo padrão para chat completion
OPENAI_MODEL_DEFAULT=gpt-4.1-mini

# Modelos permitidos (separados por vírgula)
OPENAI_ALLOWED_MODELS=gpt-4o-mini,gpt-4.1,gpt-4.1-mini,gpt-4.1-nano,gpt-5

# Base URL da OpenAI (deixe vazio para usar a padrão)
OPENAI_API_BASE=https://api.openai.com/v1

# ==================== LLM CONFIGURATION ====================
# Timeout para chamadas de LLM (segundos)
ORKIO_LLM_TIMEOUT=60

# Número de tentativas de retry para LLM
ORKIO_LLM_RETRIES=2

# ==================== LOGGING ====================
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================== UPLOAD ====================
# Diretório para armazenar uploads de arquivos
UPLOAD_DIR=/home/ubuntu/orkio/uploads

# Tamanho máximo de arquivo (em bytes, padrão: 50MB)
MAX_FILE_SIZE=52428800

# ==================== RENDER DEPLOYMENT ====================
# Porta em que o servidor vai rodar (Render usa 10000 por padrão)
PORT=10000

# Host para bind
HOST=0.0.0.0
